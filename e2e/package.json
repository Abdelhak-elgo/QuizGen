{
  "name": "@workspace/e2e",
  "private": true,
  "version": "0.0.1",
  "description": "Tests E2E Playwright — QuizGen",
  "scripts": {
    "test":       "playwright test",
    "test:ci":    "playwright test --reporter=github,html",
    "test:headed": "playwright test --headed",
    "report":     "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "@types/node": "^20.0.0"
  }
}
