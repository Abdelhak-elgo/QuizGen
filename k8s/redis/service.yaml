apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: quizgen
  labels:
    app: redis
    app.kubernetes.io/part-of: quizgen-platform
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
