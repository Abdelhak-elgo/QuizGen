apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: quizgen
  labels:
    app: frontend
    app.kubernetes.io/part-of: quizgen-platform
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
