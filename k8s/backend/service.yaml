apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: quizgen
  labels:
    app: backend
    app.kubernetes.io/part-of: quizgen-platform
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
